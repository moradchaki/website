<div class="p-6 rounded-3xl bg-surface-container-lowest border border-outline-variant/30 shadow-premium">
    <div class="flex items-center justify-between mb-4">
        <span class="material-symbols-outlined text-3xl text-{{ color }}" data-icon="{{ icon }}">{{ icon }}</span>
    </div>
    <p class="font-headline-xl text-headline-xl text-on-surface font-bold">{{ value }}</p>
    <p class="text-on-surface-variant font-body-md mt-1">{{ label }}</p>
</div>
